# 배포 가이드: Vercel & Supabase 🚀

이 프로젝트는 **Next.js**와 **Supabase**를 사용하므로, 가장 쉽고 강력한 배포 방법은 **Vercel**을 사용하는 것입니다.

## 1. Vercel 배포 (가장 추천)

Vercel은 Next.js를 만든 팀에서 운영하는 플랫폼으로, GitHub과 연동하면 코드 푸시 시 자동으로 배포됩니다.

### 단계별 절차:
1.  [Vercel](https://vercel.com)에 로그인합니다 (GitHub 계정 사용 권장).
2.  **"Add New"** -> **"Project"**를 클릭합니다.
3.  이미 연동된 `blog_writer` 저장소를 선택하여 **"Import"**합니다.
4.  **Environment Variables** 섹션에서 다음 항목을 추가합니다:
    -   `NEXT_PUBLIC_SUPABASE_URL`: Supabase 프로젝트 URL
    -   `NEXT_PUBLIC_SUPABASE_ANON_KEY`: Supabase Anon Key
5.  **"Deploy"** 버튼을 누르면 끝! 3~5분 후 실제 URL이 나옵니다.

## 2. Supabase 설정 확인

웹에 배포된 후에도 기능이 잘 작동하려면 다음 사항을 확인해야 합니다.

-   **Authentication 설정**:
    -   Supabase 대시보드 -> Authorization -> **Redirect URLs**에 Vercel에서 발급받은 실제 URL을 추가해야 로그인이 작동합니다. (예: `https://your-app-name.vercel.app`)
-   **데이터베이스 정책 (RLS)**:
    -   우리가 작성한 `supabase/final_setup_all.sql`이 Supabase SQL Editor에서 실행되어 있는지 다시 한번 확인하세요.

## 3. 다른 대안들
-   **Netlify**: Vercel과 유사하며 설정이 간편합니다.
-   **AWS Amplify**: AWS 환경을 선호할 경우 좋은 선택지입니다.

> [!TIP]
> **Vercel**을 사용하면 `main` 브랜치에 코드를 올릴 때마다 자동으로 사이트가 업데이트되어 매우 편리합니다!
